---
phase: 01.5-agentic-voice-core
plan: 03
type: execute
wave: 2
depends_on:
  - 01.5-01
  - 01.5-02
files_modified:
  - backend/app/agents/memory.py
  - backend/app/agents/tool.py
  - backend/app/agents/__init__.py
autonomous: true
requirements:
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "MockMemoryStore implements MemoryStore interface with get_history() returning empty list (no-op in Phase 1.5)"
    - "DynamoDB voicebot_sessions table schema defined with PK=session_id, SK=hashed_timestamp, TTL=90 days"
    - "MockToolAgent returns canned responses for property_tax, utility_services, permits intents"
    - "Tool responses are predictable and deterministic for testing"
    - "Both mock agents have correct interface signatures for Phase 2.5/Phase 5 real implementations"

  artifacts:
    - path: "backend/app/agents/memory.py"
      provides: "MemoryStore ABC and MockMemoryStore implementation"
      exports: ["MemoryStore", "MockMemoryStore"]
      min_lines: 40
    - path: "backend/app/agents/tool.py"
      provides: "ToolAgent ABC and MockToolAgent implementation"
      exports: ["ToolAgent", "MockToolAgent", "ToolResult"]
      min_lines: 60
    - path: "infra/terraform/session_table.py"
      provides: "DynamoDB table creation script (if created)"
      exports: []

  key_links:
    - from: "backend/app/agents/memory.py"
      to: "DynamoDB voicebot_sessions table"
      via: "boto3 dynamodb resource"
      pattern: "dynamodb.Table\\('voicebot_sessions"
    - from: "backend/app/agents/tool.py"
      to: "MockToolAgent canned responses"
      via: "CANNED_RESPONSES dict"
      pattern: "CANNED_RESPONSES.*property_tax"
    - from: "backend/app/agents/memory.py"
      to: "Hashed timestamp SK generation"
      via: "hashlib.md5 prefix calculation"
      pattern: "hashlib\\.md5\\(session_id"

---

<objective>
Create MockMemoryStore (interface-compatible no-op) and MockToolAgent (canned responses). Define DynamoDB voicebot_sessions schema for Phase 2.5 real memory implementation. Both classes implement abstract interfaces for easy Phase 2.5/5 upgrades.

Purpose: Foundation for agent memory and tool execution. Mock implementations enable Phase 1.5 to run end-to-end with predictable behavior before Phase 2.5 adds real history injection.

Output: `backend/app/agents/memory.py` + `backend/app/agents/tool.py` + DynamoDB table schema documentation.
</objective>

<execution_context>
@C:/Users/shobh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.5-agentic-voice-core/01.5-CONTEXT.md
@.planning/phases/01.5-agentic-voice-core/01.5-RESEARCH.md

# DynamoDB schema reference
@backend/app/services/knowledge.py (for DynamoDB patterns)

# Agent types
@backend/app/agents/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MemoryStore ABC and MockMemoryStore</name>
  <files>backend/app/agents/memory.py</files>
  <action>
    Create memory store interface and mock implementation:

    1. Define ConversationTurn dataclass:
       - user_input (str)
       - assistant_response (str)
       - intent (str)
       - intent_confidence (float)
       - routing_target (str)
       - timestamp (str, ISO 8601)

    2. Define MemoryStore ABC with:
       - `async def get_history(session_id: str, max_turns: int = 5) -> list[ConversationTurn]`
       - `async def write_turn(session_id: str, turn: ConversationTurn) -> None`
       - `async def delete_session(session_id: str) -> None`

    3. Implement MockMemoryStore (in Phase 1.5):
       - get_history() always returns empty list [] (no history injection yet)
       - write_turn() is no-op (does nothing, returns immediately)
       - delete_session() is no-op
       - Add logging for visibility into what Phase 1.5 is doing

    4. Include docstring with Phase 2.5 upgrade path:
       ```
       Phase 2.5 will replace MockMemoryStore with DynamoDBMemoryStore that:
       - Reads/writes to voicebot_sessions DynamoDB table
       - Injects last 5 turns into Orchestrator context
       - Populates conversation metrics (session duration, turn count, etc.)
       ```

    Imports:
    - from abc import ABC, abstractmethod
    - from dataclasses import dataclass
    - from datetime import datetime
    - logging
  </action>
  <verify>
    <automated>
    python -c "
    import asyncio
    from backend.app.agents.memory import MemoryStore, MockMemoryStore, ConversationTurn

    async def test():
        mem = MockMemoryStore()
        history = await mem.get_history('session1')
        assert history == [], f'Expected empty list, got {history}'

        # write_turn should not crash
        turn = ConversationTurn(
            user_input='test',
            assistant_response='response',
            intent='property_tax',
            intent_confidence=0.95,
            routing_target='retrieval',
            timestamp='2026-03-13T12:00:00Z'
        )
        await mem.write_turn('session1', turn)
        print('MockMemoryStore works correctly')

    asyncio.run(test())
    "
    </automated>
  </verify>
  <done>MemoryStore ABC and MockMemoryStore created with async methods</done>
</task>

<task type="auto">
  <name>Task 2: Create ToolAgent ABC and MockToolAgent</name>
  <files>backend/app/agents/tool.py</files>
  <action>
    Create tool execution interface and mock implementation:

    1. Define ToolResult dataclass:
       - tool_name (str): name of the tool executed
       - success (bool): whether call succeeded
       - data (dict): returned data
       - error (str | None): error message if failed
       - latency_ms (int): execution time

    2. Define ToolAgent ABC with:
       - `async def execute(intent: str, parameters: dict) -> ToolResult`

    3. Implement MockToolAgent (in Phase 1.5):
       - CANNED_RESPONSES dict with hardcoded data:
         * property_tax: {status: "current", amount_due: 0, next_due: "2026-04-01"}
         * utility_services: {status: "active", balance: 45.23, last_payment: "2026-02-28"}
         * permits: {status: "approved", permit_id: "MOCK-2024-001", expires: "2026-12-31"}
       - execute() returns success=True, latency_ms=50 (simulated)
       - Unknown intents return success=False, error="No mock data for this intent"

    4. Include docstring with Phase 5 upgrade path:
       ```
       Phase 5 will replace MockToolAgent with RealToolAgent that:
       - Calls actual Jackson County APIs for property lookup, utility status, permits
       - Handles rate limiting and auth
       - Logs tool calls in agent traces
       ```

    Imports:
    - from dataclasses import dataclass
    - from abc import ABC, abstractmethod
    - logging
  </action>
  <verify>
    <automated>
    python -c "
    import asyncio
    from backend.app.agents.tool import ToolAgent, MockToolAgent, ToolResult

    async def test():
        tool = MockToolAgent()
        result = await tool.execute('property_tax', {})
        assert result.success == True
        assert result.tool_name == 'property_tax'
        assert result.data.get('amount_due') == 0
        assert result.latency_ms == 50
        print('MockToolAgent works correctly')

    asyncio.run(test())
    "
    </automated>
  </verify>
  <done>ToolAgent ABC and MockToolAgent created with canned responses</done>
</task>

<task type="auto">
  <name>Task 3: Document DynamoDB voicebot_sessions table schema</name>
  <files>backend/app/agents/memory.py</files>
  <action>
    Add comprehensive schema documentation to memory.py as module-level docstring comment block:

    Include:
    1. Table name: voicebot_sessions
    2. Primary key: session_id (String, PK)
    3. Sort key: hashed_timestamp (String, SK)
    4. TTL attribute: expiry_epoch (Number, seconds since epoch = now + 90 days)
    5. Hashed prefix logic (prevents hot partition):
       ```python
       import hashlib
       prefix = int(hashlib.md5(session_id.encode()).hexdigest(), 16) % 10
       sk = f"{prefix}#{timestamp_iso}"
       ```
    6. All attributes schema (from 01.5-RESEARCH.md):
       - session_id, hashed_timestamp, expiry_epoch
       - turn_number, user_input, assistant_response
       - intent, intent_confidence, routing_target
       - retrieved_doc_ids, rag_chunks_text
       - latency fields: asr_latency_ms, rag_latency_ms, orchestrator_latency_ms, response_latency_ms, total_latency_ms
       - token counts: llm_prompt_tokens, llm_completion_tokens
       - status: completion_status, follow_up_detected, fallback_triggered, clarification_requested
       - costs: compute_cost_usd, llm_cost_usd, storage_cost_usd
       - timestamp (ISO 8601)

    This is documentation only (no code to execute) — Phase 2.5 will create the actual table.
  </action>
  <verify>
    <automated>
    grep -q "voicebot_sessions" backend/app/agents/memory.py && echo "Schema documented"
    </automated>
  </verify>
  <done>DynamoDB schema documented in memory.py</done>
</task>

<task type="auto">
  <name>Task 4: Update agent module exports</name>
  <files>backend/app/agents/__init__.py</files>
  <action>
    Update __init__.py to include new agent types:

    Add imports:
    ```python
    from backend.app.agents.memory import MemoryStore, MockMemoryStore, ConversationTurn
    from backend.app.agents.tool import ToolAgent, MockToolAgent, ToolResult
    ```

    Update __all__ to include:
    - MemoryStore, MockMemoryStore, ConversationTurn
    - ToolAgent, MockToolAgent, ToolResult

    Maintains clean import pattern: `from backend.app.agents import MockMemoryStore, MockToolAgent`
  </action>
  <verify>
    <automated>
    python -c "
    from backend.app.agents import MockMemoryStore, MockToolAgent, ConversationTurn, ToolResult
    print('All mock agent exports available')
    "
    </automated>
  </verify>
  <done>__init__.py exports updated with memory and tool agents</done>
</task>

</tasks>

<verification>
After all tasks:
- `from backend.app.agents import MockMemoryStore, MockToolAgent` succeeds
- MockMemoryStore.get_history() returns empty list (Phase 1.5 behavior)
- MockToolAgent.execute('property_tax') returns ToolResult with canned data
- DynamoDB voicebot_sessions schema documented with hashed prefix logic
- Code includes upgrade path comments for Phase 2.5 (real memory) and Phase 5 (real tools)
</verification>

<success_criteria>
- MemoryStore ABC and MockMemoryStore implement async interface
- ToolAgent ABC and MockToolAgent implement async interface
- Canned responses are deterministic and predictable for testing
- DynamoDB table schema documented with all fields from 01.5-RESEARCH.md
- Hashed partition key logic documented (MD5 prefix % 10 to prevent hot partitions)
- Code includes clear migration path comments for future phases
- All types exported via __init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-agentic-voice-core/01.5-03-SUMMARY.md`
</output>
