---
phase: 01.5-agentic-voice-core
plan: "01"
subsystem: agents
tags: [bedrock, pydantic, routing, intent-classification, multi-agent, haiku]

requires:
  - phase: 01-runnable-mvp-web-voice
    provides: AwsClientBundle pattern and bedrock_runtime client used by OrchestratorAgent

provides:
  - OrchestratorAgent class with async route() method routing queries to retrieval/tool/fallback
  - RoutingDecision Pydantic model (intent, confidence, routing_target, reasoning)
  - Message Pydantic model (role, content, timestamp) for conversation history
  - backend/app/agents/ package with __init__.py exports

affects:
  - 01.5-02 (RetrievalAgent and ResponseAgent consume RoutingDecision)
  - 01.5-03 (MockMemoryStore/MockToolAgent work with Message and RoutingDecision)
  - 01.5-04 (tracer uses intent/confidence/routing_target from RoutingDecision)
  - 01.5-05 (AgentLLMAdapter calls OrchestratorAgent.route())

tech-stack:
  added: []
  patterns:
    - "OrchestratorAgent constructor receives AwsClientBundle (not raw boto3) — reuses existing client bundle pattern"
    - "Bedrock converse() called via asyncio.to_thread() for non-blocking async operation"
    - "RoutingDecision.model_validate_json() for resilient JSON parsing with Pydantic validation"
    - "Belt-and-suspenders fallback: confidence < 0.7 enforced in Python code after parsing (not just in prompt)"
    - "Markdown fence stripping before JSON parse — defensive against LLM formatting"

key-files:
  created:
    - backend/app/agents/__init__.py
    - backend/app/agents/types.py
    - backend/app/agents/orchestrator.py
  modified: []

key-decisions:
  - "Bedrock converse() API used (NOT Anthropic SDK) — consistent with Phase 1 pattern and keeps all LLM calls through AWS"
  - "Model: anthropic.claude-3-5-haiku-20241022-v1:0 — deterministic at temperature=0, low latency, low cost for routing"
  - "Fallback enforcement at Python level (confidence < 0.7 -> routing_target='fallback') plus system prompt rule — belt-and-suspenders"
  - "5-turn history window for Bedrock converse messages — balances context richness vs prompt size"
  - "max_tokens=150 for orchestrator — JSON routing decision only, no prose needed"

patterns-established:
  - "Agent pattern: constructor(AwsClientBundle), async method, error-returns-safe-fallback"
  - "All agent errors return graceful fallback RoutingDecision — voice turns never fail due to routing errors"

requirements-completed:
  - AGENT-01

duration: 3min
completed: "2026-03-13"
---

# Phase 1.5 Plan 01: OrchestratorAgent Summary

**OrchestratorAgent routing Jackson County queries to retrieval/tool/fallback via Bedrock claude-haiku with confidence-based fallback enforcement and Pydantic JSON parsing**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-13T06:34:04Z
- **Completed:** 2026-03-13T06:37:07Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `RoutingDecision` and `Message` Pydantic models defined with strict validation (`ConfigDict(validate_assignment=True)`)
- `OrchestratorAgent` with async `route()` using Bedrock converse API, temperature=0, max_tokens=150
- Confidence-based fallback enforcement: `confidence < 0.7` coerces `routing_target` to `'fallback'` in Python code after JSON parsing
- System prompt covers all 10 intent labels and 3 routing targets with explicit rules
- Error handling returns safe `RoutingDecision(intent='error', confidence=0.0, routing_target='fallback')` on any exception

## Task Commits

Each task was committed atomically:

1. **Task 1: Define Routing Decision types (RoutingDecision, Message)** - `05468c6` (feat)
2. **Task 2: Implement OrchestratorAgent with routing logic** - `43bcca2` (feat)

## Files Created/Modified

- `backend/app/agents/types.py` - RoutingDecision and Message Pydantic models with 11 intent labels and 3 routing targets
- `backend/app/agents/orchestrator.py` - OrchestratorAgent class (174 lines); Bedrock haiku routing with fallback enforcement
- `backend/app/agents/__init__.py` - Package exports: OrchestratorAgent, RoutingDecision, Message, emit_trace_event, IntentConfusionMatrix

## Decisions Made

- Used Bedrock converse() API (not Anthropic SDK) — consistent with existing aws_clients.py pattern using `bedrock_runtime` from `AwsClientBundle`
- Model ID `anthropic.claude-3-5-haiku-20241022-v1:0` — Bedrock-format model ID for haiku (from PLAN.md must_haves)
- Confidence threshold 0.7 enforced both in system prompt AND in Python code after parsing — belt-and-suspenders design
- Markdown fence stripping added to handle LLM formatting edge cases before `model_validate_json()`

## Deviations from Plan

None - plan executed exactly as written. Files were already committed from prior session work; verified all requirements met.

## Issues Encountered

None — all imports resolved cleanly, Pydantic validation works correctly, instantiation verified.

## Next Phase Readiness

- OrchestratorAgent and type definitions ready for 01.5-02 (RetrievalAgent and ResponseAgent)
- RoutingDecision consumed by all downstream agents in plans 01.5-02 through 01.5-05
- `backend/app/agents/` package structure established for remaining agent modules

---
*Phase: 01.5-agentic-voice-core*
*Completed: 2026-03-13*
