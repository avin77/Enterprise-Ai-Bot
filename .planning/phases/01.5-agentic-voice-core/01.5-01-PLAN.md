---
phase: 01.5-agentic-voice-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/agents/__init__.py
  - backend/app/agents/orchestrator.py
  - backend/app/agents/types.py
autonomous: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "OrchestratorAgent receives a user query and returns a routing decision with intent + confidence score"
    - "Confidence < 0.7 is coerced to routing_target='fallback' regardless of intent classification"
    - "Intent labels (property_tax, utility_services, permits, etc.) are logged for analytics"
    - "Routing targets (retrieval, tool, fallback) control agent pipeline flow"
    - "Orchestrator uses AWS Bedrock haiku with temperature=0 for deterministic eval reproducibility"

  artifacts:
    - path: "backend/app/agents/__init__.py"
      provides: "Agent module exports for easy import"
      exports: ["OrchestratorAgent", "RoutingDecision"]
    - path: "backend/app/agents/types.py"
      provides: "Pydantic type definitions for routing decisions"
      contains: "class RoutingDecision"
    - path: "backend/app/agents/orchestrator.py"
      provides: "OrchestratorAgent implementing routing logic"
      exports: ["OrchestratorAgent"]
      min_lines: 100

  key_links:
    - from: "backend/app/agents/orchestrator.py"
      to: "AWS Bedrock (anthropic.claude-3-5-haiku)"
      via: "boto3 bedrock client converse() call"
      pattern: "bedrock.converse\\(.*anthropic\\.claude.*haiku"
    - from: "backend/app/agents/orchestrator.py"
      to: "RoutingDecision (Pydantic model)"
      via: "JSON parsing via model_validate_json()"
      pattern: "RoutingDecision\\.model_validate_json"
    - from: "backend/app/agents/orchestrator.py"
      to: "Confidence fallback logic"
      via: "confidence < 0.7 → routing_target = 'fallback'"
      pattern: "confidence.*0\\.7.*fallback"

---

<objective>
Create OrchestratorAgent that routes each user query to retrieval, tool, or fallback path based on intent classification. Return intent label and confidence score for analytics. Enforce fallback behavior for low-confidence queries.

Purpose: Foundation for multi-agent routing. Orchestrator is the first Claude call in the pipeline and determines which agent handles the turn.

Output: `backend/app/agents/orchestrator.py` with OrchestratorAgent class and type definitions.
</objective>

<execution_context>
@C:/Users/shobh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.5-agentic-voice-core/01.5-CONTEXT.md
@.planning/phases/01.5-agentic-voice-core/01.5-RESEARCH.md

# Existing integration points
@backend/app/orchestrator/runtime.py
@backend/app/services/llm.py
@backend/app/services/aws_clients.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Routing Decision types (RoutingDecision, Message)</name>
  <files>backend/app/agents/types.py</files>
  <action>
    Create Pydantic type definitions for routing decisions:

    1. `Message` dataclass: role (user|assistant), content (str), timestamp (datetime)
    2. `RoutingDecision` model: intent (str), confidence (float 0-1), routing_target (str: retrieval|tool|fallback), reasoning (str)
    3. Use `ConfigDict(validate_assignment=True)` for strict Pydantic validation

    Include docstrings. Keep simple — no async in types module.

    Intent label options (for analytics):
    - property_tax, utility_services, permits, trash_waste, voting, court_fees, benefits, parks, emergency, general_info, out_of_scope

    Routing target options:
    - retrieval (RAG-based)
    - tool (municipal tool lookup)
    - fallback (low confidence or out of scope)
  </action>
  <verify>
    import backend.app.agents.types as t
    assert hasattr(t, 'RoutingDecision')
    assert hasattr(t, 'Message')
    d = t.RoutingDecision(intent='property_tax', confidence=0.95, routing_target='retrieval', reasoning='test')
    assert d.confidence == 0.95
  </verify>
  <done>types.py created with RoutingDecision and Message models, validated by Pydantic</done>
</task>

<task type="auto">
  <name>Task 2: Implement OrchestratorAgent with routing logic</name>
  <files>backend/app/agents/orchestrator.py</files>
  <action>
    Create OrchestratorAgent class with async route() method:

    1. Constructor: receive `aws_clients: AwsClientBundle` (NOT direct boto3 client — reuse existing pattern)
    2. Lazy-load Bedrock client via aws_clients.bedrock()
    3. Use model ID: `anthropic.claude-3-5-haiku-20241022-v1:0` (from 01.5-RESEARCH.md standard_stack)
    4. System prompt: Include intent labels (10 options above), routing targets, confidence thresholds, reasoning requirement
    5. Temperature = 0 (deterministic for evals)
    6. max_tokens = 150 (JSON routing decision only)

    Method signature:
    ```
    async def route(self, query: str, history: list[Message] = None) -> RoutingDecision:
    ```

    Implementation:
    - If history is None, use empty list
    - Build messages: history[-5:] + user query (5-turn window per 01.5-RESEARCH.md)
    - Call bedrock.converse() with system prompt + messages
    - Parse response as JSON via `RoutingDecision.model_validate_json(response_text)`
    - After parsing, enforce fallback: if confidence < 0.7, set routing_target = 'fallback'
    - Return RoutingDecision

    Error handling:
    - If Bedrock call fails: log error, return RoutingDecision(intent='error', confidence=0.0, routing_target='fallback')
    - If JSON parsing fails: log error, return same fallback RoutingDecision

    Import statements:
    - from backend.app.agents.types import RoutingDecision, Message
    - from backend.app.services.aws_clients import AwsClientBundle
    - boto3, logging, json
  </action>
  <verify>
    <automated>
    python -c "
    import asyncio
    from backend.app.agents.orchestrator import OrchestratorAgent
    from backend.app.services.aws_clients import build_aws_clients

    async def test():
        clients = build_aws_clients()
        agent = OrchestratorAgent(clients)
        # Should not crash on instantiation
        assert agent is not None
        print('OrchestratorAgent instantiated successfully')

    asyncio.run(test())
    "
    </automated>
  </verify>
  <done>OrchestratorAgent created with async route() method, fallback enforcement, JSON parsing via Pydantic, error handling</done>
</task>

</tasks>

<verification>
After both tasks:
- `python -c "from backend.app.agents.orchestrator import OrchestratorAgent; from backend.app.agents.types import RoutingDecision"` succeeds (no import errors)
- RoutingDecision Pydantic model validates: `confidence` is float 0-1, `routing_target` is one of {retrieval, tool, fallback}
- OrchestratorAgent has `async route(query, history)` method
- System prompt in orchestrator.py includes all 10 intent labels and 3 routing targets
- Fallback enforcement logic: `if confidence < 0.7: routing_target = 'fallback'`
</verification>

<success_criteria>
- OrchestratorAgent and RoutingDecision types created and importable
- System prompt enforces routing rules (retrieval for info, tool for transactional, fallback for out-of-scope)
- Confidence < 0.7 coerces routing_target to fallback regardless of intent
- JSON parsing resilient to malformed responses
- Code follows existing project patterns (async, AwsClientBundle pattern, boto3 logging)
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-agentic-voice-core/01.5-01-SUMMARY.md`
</output>
