---
phase: 00-learning-mvp-bootstrap
plan: 05
type: execute
wave: 1
depends_on:
  - 00-04
files_modified:
  - scripts/aws-bootstrap.ps1
  - tests/e2e/test_aws_dev_deploy_smoke.py
  - .planning/phases/00-learning-mvp-bootstrap/00-VERIFICATION.md
  - .planning/phases/00-learning-mvp-bootstrap/00-UAT.md
autonomous: true
gap_closure: true
requirements:
  - PLAT-00
user_setup:
  - service: aws
    why: "Live smoke deployment requires an authenticated AWS account and CLI profile."
    env_vars:
      - name: AWS_REGION
        source: "Set to us-east-1 for phase-0 acceptance."
      - name: AWS_PROFILE
        source: "Configured AWS CLI profile with ECR/ECS/IAM/EC2 networking permissions."
      - name: AWS_SUBNET_IDS
        source: "One or more public subnet IDs for Fargate task ENIs."
      - name: AWS_SECURITY_GROUP_IDS
        source: "Security group IDs allowing inbound app traffic and outbound AWS egress."
must_haves:
  truths:
    - Phase-0 AWS validation can be executed end-to-end with AWS CLI tooling and Docker, without Terraform as an acceptance dependency.
    - AWS smoke validation can run in a short-lived deploy window and teardown cleanly to minimize cost.
    - Phase-0 verification evidence reflects CLI-first deploy and teardown behavior.
  artifacts:
    - path: scripts/aws-bootstrap.ps1
      provides: CLI-driven deploy/smoke/teardown automation for ECS/Fargate in us-east-1
    - path: tests/e2e/test_aws_dev_deploy_smoke.py
      provides: Automated checks for CLI-first AWS bootstrap path and optional live health smoke
    - path: .planning/phases/00-learning-mvp-bootstrap/00-VERIFICATION.md
      provides: Updated phase-0 evidence aligned to CLI-first acceptance
    - path: .planning/phases/00-learning-mvp-bootstrap/00-UAT.md
      provides: Updated manual checks for deploy-then-teardown path
  key_links:
    - from: scripts/aws-bootstrap.ps1
      to: tests/e2e/test_aws_dev_deploy_smoke.py
      via: Smoke tests assert script behavior and optional live health checks
    - from: scripts/aws-bootstrap.ps1
      to: .planning/phases/00-learning-mvp-bootstrap/00-VERIFICATION.md
      via: Verification evidence references CLI-first deploy and teardown flow
---

<objective>
Close phase-0 deployment strategy drift by replacing Terraform-required validation with a CLI-first AWS deployment path.

Purpose: Align implementation and verification with locked context decisions (AWS CLI-only, ECS/Fargate, ephemeral smoke deploys).
Output: Updated bootstrap script, smoke tests, and phase-0 verification/UAT docs that enforce deploy -> smoke -> teardown.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-CONTEXT.md
@.planning/phases/00-learning-mvp-bootstrap/00-04-PLAN.md
@.planning/phases/00-learning-mvp-bootstrap/00-04-SUMMARY.md
@.planning/phases/00-learning-mvp-bootstrap/00-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AWS bootstrap to CLI-first ephemeral workflow</name>
  <files>scripts/aws-bootstrap.ps1</files>
  <action>Refactor the bootstrap script so phase-0 acceptance no longer depends on Terraform commands. Keep ECS/Fargate runtime, ECR image push, and IAM-role-based task execution. Add explicit deploy and teardown modes to support short-lived smoke runs (deploy, validate, destroy). Ensure networking parameters are provided via script arguments or environment variables and that script output includes enough identifiers for smoke and teardown operations.</action>
  <verify><automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "Get-Help .\\scripts\\aws-bootstrap.ps1 -ErrorAction SilentlyContinue | Out-Null; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated></verify>
  <done>Bootstrap script can drive AWS CLI-based deployment and teardown without requiring Terraform binary or Terraform apply as a mandatory step.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke automation for CLI-first AWS path</name>
  <files>tests/e2e/test_aws_dev_deploy_smoke.py</files>
  <action>Update smoke tests to verify the AWS deploy path is CLI-first and ECS/Fargate-based. Keep optional live health check behavior gated by environment variable, and validate script contains deploy and teardown command paths. Remove Terraform-validate requirement from acceptance checks so local validation can pass without Terraform installed.</action>
  <verify><automated>python -m unittest tests/e2e/test_aws_dev_deploy_smoke.py -q</automated></verify>
  <done>Smoke test suite passes locally while enforcing CLI-first deploy expectations and retaining optional live AWS validation.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh phase-0 verification and UAT evidence for deploy/teardown</name>
  <files>.planning/phases/00-learning-mvp-bootstrap/00-VERIFICATION.md, .planning/phases/00-learning-mvp-bootstrap/00-UAT.md</files>
  <action>Update phase-0 verification and UAT docs so acceptance language reflects AWS CLI-only deployment strategy, ECS/Fargate runtime, and cost-aware ephemeral usage (deploy for smoke then teardown). Preserve existing API/auth/adapter acceptance truths and add explicit operator steps for live smoke execution prerequisites.</action>
  <verify><automated>python -m unittest tests/backend/test_backend_contracts.py tests/backend/test_orchestration_pipeline.py tests/e2e/test_phase0_roundtrip.py tests/e2e/test_aws_dev_deploy_smoke.py -q</automated></verify>
  <done>Verification artifacts accurately describe how phase-0 AWS validation is performed and what evidence proves completion.</done>
</task>

</tasks>

<verification>
- [ ] AWS bootstrap script supports CLI-first deploy and teardown workflow without Terraform as a mandatory dependency
- [ ] AWS smoke test suite validates the updated bootstrap contract
- [ ] Combined phase-0 regression suite passes after updates
- [ ] Verification/UAT docs reflect ephemeral deploy -> smoke -> teardown guidance
</verification>

<success_criteria>
- All tasks completed
- Phase 0 keeps its fixed scope (no DB, no separate auth server) while using CLI-first AWS deployment validation
- AWS validation workflow is repeatable and cost-aware for ad hoc smoke tests
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-05-SUMMARY.md`
</output>
