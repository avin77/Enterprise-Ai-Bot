---
phase: 00-learning-mvp-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-02
files_modified:
  - frontend/index.html
  - frontend/app.js
  - frontend/api_client.js
  - tests/e2e/test_phase0_roundtrip.py
autonomous: true
requirements:
  - VOIC-01
  - VOIC-02
  - API-00
user_setup: []
must_haves:
  truths:
    - Frontend uses backend `/ws` for streaming and `/chat` for non-streaming interactions.
    - Frontend contains no direct AWS calls or cloud credentials.
    - One full secure voice roundtrip is validated through automated e2e checks.
  artifacts:
    - path: frontend/app.js
      provides: Mic capture + WebSocket stream control flow
    - path: frontend/api_client.js
      provides: Backend-only API client abstraction
    - path: frontend/index.html
      provides: MVP interaction shell for voice/text tests
    - path: tests/e2e/test_phase0_roundtrip.py
      provides: End-to-end roundtrip and backend-only integration checks
  key_links:
    - from: frontend/api_client.js
      to: /chat
      via: REST call routing
    - from: frontend/app.js
      to: /ws
      via: WebSocket message stream
    - from: tests/e2e/test_phase0_roundtrip.py
      to: frontend/app.js
      via: Roundtrip interaction verification
---

<objective>
Wire the frontend layer to backend APIs for phase-0 interaction flow without direct cloud coupling.

Purpose: Enforce strict frontend/backend boundary and prove user-visible roundtrip behavior.
Output: Frontend interaction client, backend-only API usage, and e2e validation.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
@.planning/phases/00-learning-mvp-bootstrap/00-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement frontend backend-only communication flow</name>
  <files>frontend/index.html, frontend/app.js, frontend/api_client.js</files>
  <action>Implement frontend interaction flow that captures mic input and sends streaming messages to backend `/ws`, while using `/chat` for non-stream text requests. Ensure frontend code never imports AWS SDKs or embeds cloud credentials.</action>
  <verify><automated>python -m py_compile tests/e2e/test_phase0_roundtrip.py</automated></verify>
  <done>Frontend exclusively communicates with backend endpoints and supports one-turn interaction controls.</done>
</task>

<task type="auto">
  <name>Task 2: Add e2e checks for secure one-turn roundtrip</name>
  <files>tests/e2e/test_phase0_roundtrip.py</files>
  <action>Add end-to-end tests that verify one voice roundtrip across frontend and backend, endpoint usage correctness (`/ws`, `/chat`), and absence of direct AWS usage in frontend modules.</action>
  <verify><automated>python -m pytest tests/e2e/test_phase0_roundtrip.py -q</automated></verify>
  <done>Automated e2e validation confirms secure backend-mediated roundtrip behavior.</done>
</task>

</tasks>

<verification>
- [ ] Frontend path tests pass for `/ws` and `/chat`
- [ ] E2E roundtrip passes
- [ ] Frontend code has no direct AWS integration
</verification>

<success_criteria>
- All tasks completed
- Frontend/backend layering constraints are enforced by code and tests
- Phase-0 interaction flow is validated end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-03-SUMMARY.md`
</output>