---
phase: 00-learning-mvp-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-02
files_modified:
  - backend/app/ws/voice.py
  - backend/app/main.py
  - frontend/index.html
  - frontend/app.js
  - tests/e2e/test_one_turn_demo.py
autonomous: true
requirements:
  - VOIC-01
  - VOIC-02
user_setup: []
must_haves:
  truths:
    - Browser client can send mic/audio chunks over WebSocket and receive bot text/audio events.
    - Developer can run a one-turn end-to-end demo locally with automated smoke coverage.
  artifacts:
    - path: backend/app/ws/voice.py
      provides: WebSocket handler bridging client events and orchestrator stream
    - path: frontend/index.html
      provides: Minimal demo UI shell
    - path: frontend/app.js
      provides: Client WS event handling and one-turn control flow
    - path: tests/e2e/test_one_turn_demo.py
      provides: Automated one-turn end-to-end smoke validation
  key_links:
    - from: frontend/app.js
      to: /ws/voice
      via: Browser WebSocket client messages
    - from: backend/app/ws/voice.py
      to: backend/app/orchestrator/streaming.py
      via: Event stream invocation
    - from: backend/app/ws/voice.py
      to: frontend/app.js
      via: WS response event envelopes
---

<objective>
Connect browser and backend streaming paths into a working one-turn demo.

Purpose: Deliver the user-visible proof that phase-0 MVP bootstrap is complete.
Output: WS handler wiring, demo UI client, and E2E smoke test.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
@.planning/phases/00-learning-mvp-bootstrap/00-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire WebSocket session handler to orchestrator</name>
  <files>backend/app/ws/voice.py, backend/app/main.py</files>
  <action>Implement `/ws/voice` message handling for `start`, `audio_chunk`, and `end`. Route processed events through the streaming orchestrator and emit structured responses (`partial_text`, `bot_text`, `bot_audio_chunk`, `error`).</action>
  <verify><automated>python -m py_compile backend/app/ws/voice.py backend/app/main.py</automated></verify>
  <done>Backend WS endpoint streams one-turn response events with stable message envelopes.</done>
</task>

<task type="auto">
  <name>Task 2: Build minimal browser demo client and E2E smoke test</name>
  <files>frontend/index.html, frontend/app.js, tests/e2e/test_one_turn_demo.py</files>
  <action>Create a simple browser client with connect/start/send/end controls and streamed response rendering. Add an automated E2E smoke test that validates a full one-turn interaction and checks required event types.</action>
  <verify><automated>python -m pytest tests/e2e/test_one_turn_demo.py -q</automated></verify>
  <done>One-turn browser-to-backend demo succeeds and E2E smoke test passes.</done>
</task>

</tasks>

<verification>
- [ ] Backend WebSocket handler compiles and runs
- [ ] E2E one-turn smoke test passes
- [ ] Demo emits expected event envelope types end-to-end
</verification>

<success_criteria>
- All tasks completed
- Developer can run and observe one full local voice turn
- Phase 0 goal and success criteria are testable through automated checks
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-03-SUMMARY.md`
</output>