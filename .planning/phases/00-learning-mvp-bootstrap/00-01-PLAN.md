---
phase: 00-learning-mvp-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/chat.py
  - backend/app/security/auth.py
  - backend/app/security/rate_limit.py
  - backend/app/schemas/messages.py
  - backend/requirements.txt
  - tests/backend/test_backend_contracts.py
autonomous: true
requirements:
  - API-00
  - VOIC-01
user_setup: []
must_haves:
  truths:
    - Backend exposes `/ws`, `/chat`, and `/health` as the only public MVP interaction surface.
    - Backend enforces MVP-level token validation and rate limiting.
  artifacts:
    - path: backend/app/main.py
      provides: FastAPI app and endpoint/router registration
      exports:
        - app
    - path: backend/app/api/chat.py
      provides: REST chat endpoint handler
    - path: backend/app/security/auth.py
      provides: Token validation dependency/middleware
    - path: backend/app/security/rate_limit.py
      provides: Request throttling guard
    - path: tests/backend/test_backend_contracts.py
      provides: Automated endpoint and protection checks
  key_links:
    - from: backend/app/main.py
      to: backend/app/api/chat.py
      via: Route inclusion for `/chat`
    - from: backend/app/main.py
      to: /ws
      via: WebSocket endpoint registration
    - from: backend/app/security/auth.py
      to: backend/app/api/chat.py
      via: Auth dependency application
---

<objective>
Create the backend layer skeleton as the secure gateway for phase-0 interactions.

Purpose: Ensure all client traffic flows through backend APIs and MVP protections before orchestration work.
Output: Backend endpoint contracts, MVP auth/rate-limit guards, and backend contract tests.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend endpoint surface and schemas</name>
  <files>backend/app/main.py, backend/app/api/chat.py, backend/app/schemas/messages.py, backend/requirements.txt</files>
  <action>Implement FastAPI service skeleton that exposes `/health`, `/chat`, and `/ws`. Define request/response message schemas shared by REST and WebSocket handlers. Keep backend as the only layer that external clients talk to for MVP interactions.</action>
  <verify><automated>python -m py_compile backend/app/main.py backend/app/api/chat.py backend/app/schemas/messages.py</automated></verify>
  <done>All required phase-0 backend endpoints exist and schema modules compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add MVP token validation and rate limiting</name>
  <files>backend/app/security/auth.py, backend/app/security/rate_limit.py, tests/backend/test_backend_contracts.py</files>
  <action>Add lightweight token validation and request rate limiting for `/chat` and `/ws` paths. Validate these protections with automated tests to confirm protected endpoints reject missing/invalid tokens and enforce throttle behavior.</action>
  <verify><automated>python -m pytest tests/backend/test_backend_contracts.py -q</automated></verify>
  <done>MVP API protection checks pass and backend endpoints are guarded for phase-0 validation.</done>
</task>

</tasks>

<verification>
- [ ] Required backend endpoints are registered (`/health`, `/chat`, `/ws`)
- [ ] Token validation and rate limiting checks pass
- [ ] Backend contract test suite passes
</verification>

<success_criteria>
- All tasks completed
- Backend layer is the single integration surface for frontend MVP traffic
- MVP-level API protection exists without full phase-2 hardening scope
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-01-SUMMARY.md`
</output>