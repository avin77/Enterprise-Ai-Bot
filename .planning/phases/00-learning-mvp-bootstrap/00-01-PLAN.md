---
phase: 00-learning-mvp-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - backend/app/main.py
  - backend/requirements.txt
  - scripts/dev-up.ps1
  - scripts/dev-down.ps1
autonomous: true
requirements:
  - VOIC-01
user_setup: []
must_haves:
  truths:
    - Developer can start backend and supporting local services with one command.
    - Voice WebSocket endpoint is reachable for session start.
  artifacts:
    - path: docker-compose.yml
      provides: Local service topology for MVP bootstrap
    - path: backend/app/main.py
      provides: FastAPI app with health endpoint and WS registration
      exports:
        - app
    - path: scripts/dev-up.ps1
      provides: Repeatable startup command
  key_links:
    - from: scripts/dev-up.ps1
      to: backend/app/main.py
      via: Starts backend app entrypoint
    - from: backend/app/main.py
      to: /ws/voice
      via: Router registration
---

<objective>
Establish a repeatable local runtime baseline for the learning MVP.

Purpose: Remove setup friction so every subsequent plan can focus on feature behavior.
Output: Local startup scripts, backend service skeleton, and reachable WS endpoint.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create local runtime skeleton</name>
  <files>docker-compose.yml, backend/requirements.txt, backend/app/main.py</files>
  <action>Create a minimal local stack for backend development. Define FastAPI app bootstrap with `/health` endpoint and register a placeholder `/ws/voice` route. Keep dependencies minimal and phase-0 focused.</action>
  <verify><automated>docker compose config</automated></verify>
  <done>Compose file validates and backend app entrypoint imports without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable startup/shutdown scripts</name>
  <files>scripts/dev-up.ps1, scripts/dev-down.ps1</files>
  <action>Add scripts to start and stop local phase-0 services consistently. Ensure script output clearly indicates successful backend startup and endpoint URLs.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/dev-up.ps1 -DryRun</automated></verify>
  <done>Startup and shutdown scripts run without syntax errors and document expected runtime endpoints.</done>
</task>

</tasks>

<verification>
- [ ] docker compose config passes
- [ ] python -m py_compile backend/app/main.py passes
- [ ] Startup script dry-run succeeds
</verification>

<success_criteria>
- All tasks completed
- Local bootstrap path is reproducible from clean repo state
- WS endpoint route exists and is discoverable for next plan
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-01-SUMMARY.md`
</output>