---
phase: 00-learning-mvp-bootstrap
plan: 04
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-02
files_modified:
  - backend/Dockerfile
  - infra/terraform/main.tf
  - infra/terraform/variables.tf
  - infra/terraform/outputs.tf
  - scripts/aws-bootstrap.ps1
  - tests/e2e/test_aws_dev_deploy_smoke.py
autonomous: true
requirements:
  - PLAT-00
user_setup:
  - service: aws
    why: "AWS account access is required for actual deployment execution"
    env_vars:
      - name: AWS_REGION
        source: "Set to us-east-1"
      - name: AWS_PROFILE
        source: "Configured AWS CLI profile with ECR/ECS/Terraform permissions"
must_haves:
  truths:
    - Backend MVP container can be built and deployed to AWS us-east-1 dev environment.
    - AWS deploy path is scriptable and repeatable from repository assets.
  artifacts:
    - path: backend/Dockerfile
      provides: Backend container image definition
    - path: infra/terraform/main.tf
      provides: Minimal AWS bootstrap resources for MVP deployment
    - path: scripts/aws-bootstrap.ps1
      provides: Deploy workflow automation for us-east-1
    - path: tests/e2e/test_aws_dev_deploy_smoke.py
      provides: AWS deployment smoke verification
  key_links:
    - from: backend/Dockerfile
      to: infra/terraform/main.tf
      via: Container artifact deployed through Terraform-managed resources
    - from: scripts/aws-bootstrap.ps1
      to: infra/terraform/main.tf
      via: Infrastructure apply orchestration
    - from: tests/e2e/test_aws_dev_deploy_smoke.py
      to: scripts/aws-bootstrap.ps1
      via: Post-deploy health verification
---

<objective>
Provide a minimal AWS deployment path for phase-0 backend validation in us-east-1.

Purpose: Satisfy AWS-first delivery requirement without pulling full phase-2 hardening scope forward.
Output: Containerization, Terraform bootstrap, deploy script, and cloud smoke test.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
@.planning/phases/00-learning-mvp-bootstrap/00-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add container and Terraform bootstrap for AWS deploy</name>
  <files>backend/Dockerfile, infra/terraform/main.tf, infra/terraform/variables.tf, infra/terraform/outputs.tf</files>
  <action>Create backend Docker image definition and minimal Terraform resources for AWS us-east-1 MVP deployment (ECR plus backend runtime target). Include IAM role usage and environment-driven configuration without frontend credential exposure.</action>
  <verify><automated>terraform -chdir=infra/terraform validate</automated></verify>
  <done>Terraform configuration validates and backend container artifact is deploy-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AWS bootstrap script and cloud smoke test</name>
  <files>scripts/aws-bootstrap.ps1, tests/e2e/test_aws_dev_deploy_smoke.py</files>
  <action>Implement scriptable AWS bootstrap for us-east-1 using configured profile/environment and add smoke test validating deployed backend health endpoint and basic request path.</action>
  <verify><automated>python -m pytest tests/e2e/test_aws_dev_deploy_smoke.py -q</automated></verify>
  <done>AWS deploy bootstrap is repeatable and verified by automated smoke test command.</done>
</task>

</tasks>

<verification>
- [ ] terraform validate succeeds
- [ ] Backend container build definition is valid
- [ ] AWS deploy smoke test command succeeds in configured environment
</verification>

<success_criteria>
- All tasks completed
- Phase 0 includes practical AWS deployment validation path
- Cloud bootstrap remains lightweight and does not absorb phase-2 hardening scope
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-04-SUMMARY.md`
</output>