---
phase: 00-learning-mvp-bootstrap
plan: 04
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-02
files_modified:
  - infra/terraform/main.tf
  - infra/terraform/variables.tf
  - infra/terraform/outputs.tf
  - backend/Dockerfile
  - scripts/aws-bootstrap.ps1
  - tests/e2e/test_aws_deploy_smoke.py
autonomous: true
requirements:
  - PLAT-00
user_setup:
  - service: aws
    why: "AWS account credentials and target account/region selection require user-owned access"
    env_vars:
      - name: AWS_REGION
        source: "Set to us-east-1 for this project"
      - name: AWS_PROFILE
        source: "Local AWS CLI named profile with deploy permissions"
must_haves:
  truths:
    - MVP backend can be deployed to AWS us-east-1 for development validation.
    - Deployment path is repeatable from repository scripts and IaC files.
  artifacts:
    - path: infra/terraform/main.tf
      provides: AWS bootstrap infrastructure definitions for phase-0 backend deployment
    - path: backend/Dockerfile
      provides: Container image definition for backend service
    - path: scripts/aws-bootstrap.ps1
      provides: Automated deploy bootstrap flow for us-east-1
    - path: tests/e2e/test_aws_deploy_smoke.py
      provides: Deployment smoke verification command
  key_links:
    - from: backend/Dockerfile
      to: infra/terraform/main.tf
      via: Container artifact consumed by AWS deployment resources
    - from: scripts/aws-bootstrap.ps1
      to: infra/terraform/main.tf
      via: Terraform init/plan/apply invocation
    - from: tests/e2e/test_aws_deploy_smoke.py
      to: scripts/aws-bootstrap.ps1
      via: Smoke test verifies deployed endpoint after bootstrap
---

<objective>
Add a minimal AWS deployment path in us-east-1 for the phase-0 backend MVP.

Purpose: Ensure cloud execution starts early because AWS is a hard project constraint.
Output: Terraform bootstrap files, backend container build path, deploy script, and AWS smoke test.
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-learning-mvp-bootstrap/00-RESEARCH.md
@.planning/phases/00-learning-mvp-bootstrap/00-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AWS bootstrap IaC and container packaging</name>
  <files>infra/terraform/main.tf, infra/terraform/variables.tf, infra/terraform/outputs.tf, backend/Dockerfile</files>
  <action>Define minimal AWS us-east-1 deployment resources for backend MVP and container image packaging. Keep scope phase-0 minimal (deployable service path only), and avoid full security/compliance hardening already planned for later phases.</action>
  <verify><automated>terraform -chdir=infra/terraform validate</automated></verify>
  <done>Terraform config validates and backend container build definition exists for AWS deployment path.</done>
</task>

<task type="auto">
  <name>Task 2: Add deployment automation and AWS smoke test</name>
  <files>scripts/aws-bootstrap.ps1, tests/e2e/test_aws_deploy_smoke.py</files>
  <action>Create a repeatable script for AWS bootstrap deployment in us-east-1 and add a smoke test that validates deployed backend health/WS reachability. Include clear script parameters for profile and region.</action>
  <verify><automated>python -m pytest tests/e2e/test_aws_deploy_smoke.py -q</automated></verify>
  <done>AWS bootstrap script and smoke test enable repeatable cloud validation for phase-0 backend MVP.</done>
</task>

</tasks>

<verification>
- [ ] terraform validate passes for infra/terraform
- [ ] backend container build definition is valid
- [ ] AWS deploy smoke test command passes in configured environment
</verification>

<success_criteria>
- All tasks completed
- Phase 0 includes a runnable AWS deployment path in us-east-1
- Local MVP and AWS MVP paths are both documented and testable
</success_criteria>

<output>
After completion, create `.planning/phases/00-learning-mvp-bootstrap/00-04-SUMMARY.md`
</output>